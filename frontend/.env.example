# Frontend environment variables example
# Copy to .env (or .env.local) and set values before starting the frontend dev server

# Base API url used by the frontend to contact the backend (include /api if desired)
# For local dev, the backend default is http://localhost:5000
VITE_API_URL=http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api
